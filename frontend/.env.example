# 프론트엔드가 호출할 백엔드 API 주소.
# 로컬은 아래 기본값을 그대로 쓰고, 배포 시엔 Render 백엔드 URL로 바꾼다.
#   예) VITE_API_BASE_URL=https://careerfit-ai-xxxx.onrender.com
# ※ Vite는 이 값을 "빌드 시점"에 넣으므로, 값 변경 후엔 다시 빌드/재배포해야 한다.
VITE_API_BASE_URL=http://localhost:8000
