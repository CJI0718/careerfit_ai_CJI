# 여러 LLM provider 호출용 (Mistral REST 호출에 필요)
# 임베딩은 ChromaDB 기본 모델(all-MiniLM, onnxruntime)을 사용 → torch 불필요(가벼움).
chromadb==0.5.23
fastapi==0.115.5
google-generativeai==0.8.3
pandas==2.2.3
pydantic==2.10.3
python-dotenv==1.0.1
requests==2.34.2
requests-oauthlib==2.0.0
uvicorn==0.32.1